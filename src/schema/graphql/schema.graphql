type Textbook {
  id: ID
  name: String!
  publisher: String!
  category: String
}

type Query {
  textbook(id: ID!): Textbook!
  textbooks: [Textbook]!
}

type Mutation {
  addTextbook(name: String!, publisher: String!, category: String): Textbook
  deleteTextbook(id: ID!): Textbook
}
